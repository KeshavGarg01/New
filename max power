#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    long long int n,x;
    int count=0,a=2;
    cin>>n;
    cin>>x;
    /*long int y;
    y=0;
    for(int i=0;i<n;i++)
    {
        y  = y + (x%10 * pow(2,i));
        x=x/10 ;
    }
   //cout<<y<<endl;*/
   
    for(int i=0;i<n;i++)
    {
        a=pow(2,i);
        if(x%a==0)
        {
            count=i;
        }
    }
    cout<<count<<endl;
    
    return 0;
    
    
}
